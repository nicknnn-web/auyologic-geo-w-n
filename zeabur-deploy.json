{
  "buildCommand": "npm install && npm run build",
  "outputDir": "dist",
  "installCommand": "npm install",
  "port": 3000,
  "env": {
    "VITE_API_URL": {
      "description": "后端 API 地址",
      "required": true
    }
  }
}
