# 品牌 AI 健康体检报告 · 数据逻辑说明文档

> 本文档面向产品 / 运营 / 研发新同学，说明报告各模块的**数据来源**、**计算口径**与**展示含义**。  
> 实现入口：**后端聚合** `backend/src/routes/geoHealthReport.js`（`GET /api/geo-health-report`）；**前端** `frontend/src/views/GEOHealthReport.vue`。

---

## 一、端到端数据流水线（先看全貌）

一次完整体检不是「只写一张表」，而是**探针 → 入库 → 二次分析 → 报告聚合**四段：

```
用户发起「生成体检报告」
    ↓
① 抽题：从「问题库」按 keyword_type（sys_dict）每类抽若干条 → 写入 geo_health_question
    （条数配置：GEO_HEALTH_QUESTIONS_PER_TYPE，默认每类 10，见 backend/src/config/geoBrandTaskConfig.js）
    ↓
② 探针：对「每道题 × 任务勾选的多条大模型连接」分别调用 AI → 写入 geo_health_answer（原始 JSON）
         → 解析回答里的 sources/articles → 去重写入 geo_health_article（引用条目）
    （实现：backend/src/services/geoBrandTaskService.js）
    ↓
③ 二次分析：逐条读取 geo_health_answer 中的回答正文，再调「分析模型」打多维标签
             → 写入 geo_health_analysis（与 answer 一对一；失败行可有 error_text）
    （实现：backend/src/services/geoBrandAnalysisService.js；Prompt：backend/src/prompts/geoHealthAnalysis.js）
    ↓
④ 报告聚合：GET /api/geo-health-report 读取「当前用户下最新已完成任务」的全部 geo_health_analysis（error_text 为空）
             做 KPI、矩阵、竞品、词云等运算 → JSON 给前端；**命中 `geo_task_cache` 且指纹未变时直接返回快照（含词云），跳过重算**
```

**统计粒度：** 报告中绝大多数指标的分母是 **`geo_health_analysis` 中分析成功的行数**（`error_text IS NULL`），即「题 × 模型」级别；不是单纯的「题目数」。

---

## 二、涉及的核心表（报表与各模块各自主要读哪张表）

| 表名 | 作用 |
|------|------|
| `geo_health_task` | 任务头：`keyword`（创建时的品牌/企业名）、`connection_ids`（探针模型）、`analysis_connection_id`（分析模型）、`status`（completed 才参与报告） |
| `geo_health_question` | 本次抽检题干：`question`、`question_type`（对应 sys_dict 的 `data_key`） |
| `geo_health_answer` | 探针原始结果：`model_name`、`raw_json`（含回答正文，词云统计用）、`error_text`（探针失败） |
| `geo_health_article` | AI 回答中解析出的引用条目：`source_category`（四分类，信源饼图用）、`title`/`url` 等 |
| `geo_health_analysis` | 二次分析结果：**visibility / position / brand_status / compare_status / has_negative / competitors_mentioned / source_type** 等；**绝大部分 KPI 与矩阵直接读本表** |
| `geo_task_cache` | **报告快照**：按 `task_id` 唯一一行；存整份 `report_payload`（与接口 JSON 一致）及 `sentiment_word_cloud`；**分析行数+最后分析时间、文章行数+最后入库时间、启用词库条数+max(id)+max(updated_at)** 三元组作指纹，任一变化则下次 GET 重算并覆盖缓存。环境变量 `GEO_TASK_CACHE=0` 关闭读/写缓存。 |

---

## 三、报告选用「哪一次任务」的数据

接口逻辑（`geoHealthReport.js`）：

- 取 `users` 中当前 `x-user-id` 的 **企业名称** `company_name`，与任务表 `geo_health_task.keyword` 对齐（任务创建时写入）。
- 在 **`status = 'completed'`** 且「该任务下已有 `geo_health_analysis`」的任务中，按 **`geo_health_analysis.created_at` 最晚** 的任务取 **一条** 作为整页数据源。
- 若 `keyword` 为空（历史数据），仍可能与当前企业名称匹配。

因此：**报告永远是「该企业名下最近一次分析完成的体检任务」的全量聚合**，不是多任务混合。

---

## 四、`category`（brand / compare / open）怎么来的

**不由分析 AI 输出**，在入库分析结果前由代码 **`inferCategory(question_type, keyword_type_label)`** 判定（`geoBrandAnalysisService.js`）：

1. **优先**用 sys_dict 里 `keyword_type` 的 **`data_value`（展示文案）** 做关键词匹配：  
   - 含「对比」「竞品」→ `compare`  
   - 含「品牌词」「核心词」或「品牌」「核心」等 → `brand`  
   - 含「企业词」→ `brand`  
   - **其余** → `open`
2. **无字典文案时**按 `question_type`（`data_key`）兜底，例如默认种子：`01→brand`，`02→open`，`03→open`，`04→brand`，`05→compare`，`06→open`（与库内种子一致；若你们改过字典，以运行时的 `inferCategory` 为准）。

**含义：**  
- **`open`**：开放式意图（用于「首行心智拦截率」等 KPI 的主口径）。  
- **`brand`**：品牌向题干 → 矩阵走「品牌词」规则（`brand_status`）。  
- **`compare`**：对比向题干 → 矩阵走「对比词」规则（`compare_status`）。

> 旧文档写死的「01 核心词 / 02 对比词」可能与当前字典不一致；**以线上 `keyword_type` 字典 + 上述推断为准**。

---

## 五、模块说明（与页面模块对齐）

### 模块 A：大模型可见度综合得分（柱状图）

**数据源：** `geo_health_analysis`，`error_text IS NULL`，按 `model_name` 分组。

**公式：**

```text
某模型得分 = (该模型 visibility = 'visible' 的行数 / 该模型总行数) × 100，四舍五入到 0–100
```

**状态文案（柱子上标签）：**  
结合「可见度得分」与下方 **同一卡片上的 `healthScore`（AI 健康分）** 分档：≥70 表现良好；40–69 待加强；&lt;40 高风险（见模块 G）。

**子弹文案：** 根据该模型 `visibleCount/total` 与样本量是否 &lt;5 生成（`buildModelBullets`）。

---

### 模块 B：四大核心 KPI（首行心智拦截率、盲区指数、负面关联度、信源权威指数）

以下均在 **同一 `taskId`**、**`error_text IS NULL`** 的分析行上计算。

#### B1 首行心智拦截率（interceptRate）

- **优先分母：** `category = 'open'` 的分析行总数。  
- **分子：** 其中 `visibility = 'visible'` 的行数。  
- **公式：** `AVG(visible ? 100 : 0)`，再 clamp 到 0–100。  
- **若无开放式样本**（open 行数为 0）：**降级为全部分析行**（`kpiDenominator = 'all_fallback'`），前端可提示「开放式样本不足」类说明。

#### B2 大模型盲区指数（blindModelCount / totalModelCount + blindIndex）

- 对每个 `model_name` 单独看 **`category = 'open'`** 的行：  
  - 若 `open_total > 0` 且 **`open_blind == open_total`**（开放式全部 `not_visible`）→ 计为 **1 个盲区模型**。  
- **展示：** 分数形式「盲区模型数 / 参与模型数」；`blindIndex` 为盲区占比 ×100（供进度条）。

#### B3 负面事实关联度（negativeRatio / negativeRiskLevel）

- **分子：** `has_negative = true` 的分析行数。  
- **分母：** 全部分析行数。  
- **negativeRatio：** 0–1 小数（保留 4 位）。  
- **风险等级：** 0 健康；(0,0.1) 亚健康；[0.1,0.2) 低风险；[0.2,0.3) 高风险；≥0.3 超高风险。

#### B4 信源权威指数（authorityScore）

- **分母：** 全部分析行数 `totalChecks`（与分析成功条数一致）。  
- **分子：** `source_type` 命中「可信关键词」的行数之和。可信关键词固定包含：**官网、媒体、百科、认证、官方、新闻、期刊**（子串匹配）。  
- **公式：** `credibleCount / totalChecks × 100`。

#### B5 品牌提及率 vs 行业基准线（brandMentionRate / industryMentionRate）

仅 **`category = 'open'`** 的分析行：

- **指标 A（品牌提及率）：** `visibility = 'visible'` 行数 / open 总行数 × 100。  
- **指标 B（行业基准线）：** `(visibility = 'visible' OR competitors_mentioned 非空数组)` 行数 / open 总行数 × 100。  

**补充字段：**

- **`openMentionTotal`：** 开放式 **分析行数**（题×模型）。  
- **`openQuestionCount`：** 用 **`inferCategory(question_type, data_value) === 'open'`** 统计的**题目数**（与字典展示文案一致），用于说明「开放式题目有多少道」，与 `openMentionTotal` 含义不同。

---

### 模块 C：全域可见度矩阵（问题类型 × 模型）

**数据源：** `geo_health_analysis` 成功行；按 **`(question_type, model_name)`** 分桶，桶内可能有多行（多题）。

**行（意图路径）：** 来自 **`sys_dict.keyword_type`**（启用项），顺序与字典一致；无数据时用代码兜底键位。

**列：** 本任务出现过的 `model_name`，展示名见后端 `MODEL_DISPLAY_MAP`，并可叠加「大模型接入」里配置的 Logo。

**单元格规则（与代码一致）：**

| 大类 | 输入字段 | 规则摘要 |
|------|----------|----------|
| **open** | `position`, `has_negative` | T0=3 分，T1=1，T2=0；**T3 或 has_negative → 强制「负面风险」**；否则按均分 ≥2.5 / ≥1 / &gt;0 / 0 分档 |
| **brand** | `brand_status`, `has_negative` | accurate=3，bias=1；**hijack → 竞品劫持**；**risk 或 has_negative → 负面预警**；再按均分档 |
| **compare** | `compare_status`, `has_negative` | win=3，neutral=1，lose=0；**compare_status=hijack 或全 lose → 竞品挟持**；**risk 或 has_negative → 负面预警**；再按均分档 |

无数据单元格：`state = no_data`。

**矩阵底部汇总：** 统计各单元格 `state` 归属：绿色/蓝色类为「被拦截」；灰色缺失类为「盲区」；竞品占优/负面风险/挟持等为「竞品占优」侧（与前端映射一致）。

---

### 模块 D：竞品拦截诊断（帕累托图）

**数据源：** `geo_health_analysis.competitors_mentioned`（JSON 文本数组）逐元素展开。

- **柱高：** 某竞品名出现次数。  
- **占比 pct：** `该竞品次数 / 全竞品展开总次数 × 100`（整数百分比）。  
- **累计曲线：** 按次数降序前缀和 / 总次数。  
- **80% 虚线：** 帕累托解读用。

**点击某一竞品：** 前端请求 **`GET /api/geo-health-report/competitor?taskId=&name=`**，返回该竞品在不同 `question_type`、`model_name` 下的 **win/lose/neutral 计数**及 **has_negative** 计数，并附带各桶内**去重后的源问题**列表（题干优先来自 `geo_health_question`，否则回退 `questions` 源题）。

---

### 模块 E：AI 语义情绪（词云）

**与旧版说明的重要区别：** 词云 **不是** 简单汇总 `sentiment_keywords` 字段。

**当前实现：**

1. 读取本任务下每条分析成功记录关联的 **`geo_health_answer.raw_json`**，抽出**探针回答正文**（与二次分析使用的文本同源：`extractProbeAnswerText`）。  
2. 加载当前用户在 **`geo_sentiment_lexicon`** 中启用的词表（正 / 中 / 负三类；**每条关键词 ≤4 个 Unicode 码点**，超长词在读取时会被忽略，新建/编辑由接口与「情感词管理」页校验），在原文中做 **从左到右、非重叠子串** 匹配计数（英文词大小写不敏感）。  
3. **（补充）** 若用户已配置启用的大模型连接且未关闭环境开关 `GEO_HEALTH_WORDCLOUD_AI`，则再按 Prompt（`backend/src/prompts/geoHealthWordCloudAi.js`）让模型从每条正文中抽取 **正面优势 / 中性描述 / 负面警示** 三类短语（`polarity`：`positive` / `neutral` / `negative`）；User 侧会注入 **`users.description`（品牌简介）与 `users.target_audience`（目标受众，可选）** 的截断文案，便于理解品牌定位，**但 phrase 仍须逐字来自探针回答正文**。**短语长度 ≤4 个 Unicode 码点**，服务端丢弃超长项。`geo_task_cache` 指纹**不含**简介变更；仅改简介且命中缓存时，词云可能仍为旧快照，需待分析/词库等指纹变化后重算或关闭缓存调试。  
4. **合并规则：** 先放入本任务 **count &gt; 0** 的词库词；AI 短语若与**本任务已有命中**的词库词在合并键上重合（含拉丁大小写归一），则**不再单独统计**（避免双计），极性以词库为准。仅 AI 且确有命中次数的短语追加进词云。  
5. 仅 **count &gt; 0** 的词进入词云；**weight** = count / 当前任务全部词云项中的最大 count，用于字号。列表项可带 **`source`**：`lexicon` 或 `ai`（前端可忽略）。

**颜色：** 由 **`polarity`（正/负/中）** 决定；词库词用词表 tier；纯 AI 词用模型返回的三类之一。

**开关：** `GEO_HEALTH_WORDCLOUD_AI=0` 关闭 AI 补充，行为与旧版「仅词库」一致。可选：`GEO_HEALTH_WORDCLOUD_AI_MAX_CHARS`、`GEO_HEALTH_WORDCLOUD_AI_MAX_ANSWERS`、`GEO_HEALTH_WORDCLOUD_AI_TIMEOUT_MS` 控制分批与超时；`GEO_HEALTH_WORDCLOUD_BRAND_BRIEF_MAX_CHARS`、`GEO_HEALTH_WORDCLOUD_TARGET_AUDIENCE_MAX_CHARS` 控制简介/受众注入 Prompt 的截断长度。

**下钻列表：** **`GET /api/geo-health-report/sentiment-sources?taskId=&page=&q=`** 返回「哪道题的回答里命中了哪个**词库**词」等明细（服务端拼装，支持模糊筛选）。**注：** 主报告词云中的纯 AI 短语不在此接口展开。

---

### 模块 F：底层信源溯源（条形图 / 饼图）

**与旧版说明的重要区别：** 主报告里的 **`sourceData`** 来自 **`geo_health_article.source_category` 聚合**，不是 `geo_health_analysis.source_type` 的 Top8。

**四分类枚举（与探针解析一致）：**

| 内部值 | 展示名 |
|--------|--------|
| `authority_media` | 权威媒体 |
| `industry_vertical` | 行业垂直 |
| `official_media` | 官方自媒体 |
| `ugc_community` | UGC / 社区 |

饼图占比：**四类条数之和为分母**（缺类视为 0）；未归类入库可能记为 `industry_vertical` 等默认策略（见 `sourceClassifier.js`）。

**「采信率」类文案：** 若页面与 **KPI「信源权威指数」** 对照，后者仍基于分析行的 **`source_type` 文本** 与可信关键词（见模块 B4），与饼图的 **`geo_health_article` 分类** 口径不同，**勿混为一谈**。

**文章列表下钻：** **`GET /api/geo-health-report/source-articles?taskId=&page=&q=`**（标题、URL、分类、对应题干）。

---

### 模块 G：综合健康分（顶部大字 healthScore）

**与旧版「四项加权」公式不一致**；当前以 **`backend/src/utils/aiHealthScore.js`** 为准：

```text
记 v = 当前「卡片所用模型」的可见度得分（0–100，单模型即该柱得分）
记 ir = 任务级「首行心智拦截率」interceptRate（0–100）
记 n = 任务级 negativeRatio（0–1）
记 r = industryMentionRate > 0 ? brandMentionRate / industryMentionRate : 0

raw = v × 0.4 + ir × 0.6 − n × v − (n × r) × 100
healthScore = clamp(raw, 0, 100)
```

- 接口里 **根字段 `healthScore`** 与 **`modelVisibilityCards[0].healthScore`** 一致（按可见度排序后的第一个模型卡片）。  
- **每张模型卡片**上的 `healthScore` 用 **该模型自己的可见度** 代入 `v`，`ir/n/brandMentionRate/industryMentionRate` 仍为**任务级同一套**。

**comparePercent：** `max(5, 100 - healthScore) + '%'`（兼容字段，语义为「对比/差距」类展示）。

---

### 模块 H：智能诊断总结（diagnosticSuggestions）

**与旧版「多条硬规则表格」不一致。** 当前后端 **仅**根据 **综合 AI 语境矩阵（16 档）** 生成 **至多一条** 诊断：

- 输入：各模型可见度得分数组、`interceptRate`、`negativeRatio`。  
- 对每个模型先算 `exposure = 0.4×vis + 0.6×ir`，`penalty = negativeRatio`，映射到 16 档之一（`backend/src/utils/contextMatrix.js`）。  
- 再对多模型的 exposure / penalty **取平均**，落到同一套 16 档，得到 **`matrixContext`**（名称、摘要、建议列表）。  
- **`diagnosticSuggestions`** = 把该档位包装成一条卡片（若吸附到最近邻档位会附加说明文案）。

页面若支持编辑文案，一般为**前端本地展示**，不回写库（以产品实现为准）。

---

## 六、辅助接口（排查数据 / 对账）

| 接口 | 用途 |
|------|------|
| `GET /api/geo-health-report/task-qa?taskId=` | 本次任务全部「题 × 模型」的题干 + 探针回答原文 + 错误信息，便于与矩阵、词云对账 |
| `GET /api/geo-health-report/sentiment-sources?taskId=&page=&q=` | 词云命中明细 |
| `GET /api/geo-health-report/source-articles?taskId=&page=&q=` | 引用文章明细 |
| `GET /api/geo-health-report/competitor?taskId=&name=` | 单个竞品详情 |

---

## 七、常见「看不懂」的问题

1. **为什么 KPI 分母和题目数不一致？**  
   多为「每题测了多个模型」，分析行数 = 题 × 模型；部分 KPI 还用 `category='open'` 再滤一层。

2. **为什么开放式题目数为 `openQuestionCount`，但开放式提及总行数是 `openMentionTotal`？**  
   前者是**题**，后者是**分析行（题×模型）**。

3. **探针失败或分析失败的题会怎样？**  
   探针失败：`geo_health_answer.error_text` 有值，可能无分析行或分析行带错。  
   报告聚合 **忽略** `geo_health_analysis.error_text IS NOT NULL` 的行，故失败题不参与 KPI/矩阵。

4. **词云没有词？**  
   检查 `geo_sentiment_lexicon` 是否配置；或探针 `raw_json` 是否无正文。

---

## 八、附：数据流简图

```
问题库 ──抽题──► geo_health_question
                    │
多模型探针 ───────► geo_health_answer + geo_health_article
                    │
分析模型打标 ─────► geo_health_analysis
                    │
GET /api/geo-health-report ──► 前端各模块
```

---

*文档更新：2026-05-08 · 主要对齐代码：`backend/src/routes/geoHealthReport.js`、`backend/src/services/geoBrandAnalysisService.js`、`backend/src/utils/aiHealthScore.js`、`backend/src/utils/contextMatrix.js`、`backend/src/services/sentimentLexiconService.js`*
