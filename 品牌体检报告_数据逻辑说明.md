# 品牌 AI 健康体检报告 · 数据逻辑说明文档

> 本文档面向产品 / 运营 / 研发新同学，说明报告各模块的**数据来源**、**计算口径**与**展示含义**。  
> 实现入口：**后端聚合** `backend/src/routes/geoHealthReport.js`（`GET /api/geo-health-report`）；**任务编排与中止** `backend/src/routes/geoBrandTask.js`；**前端** `frontend/src/views/GEOHealthReport.vue`。

---

## 一、端到端数据流水线（先看全貌）

一次完整体检不是「只写一张表」，而是**探针 → 入库 → 二次分析 → 词云快照入库 → 报告聚合**五段（前四段与「词云」在数据上的衔接见模块 E）：

```
用户发起「生成体检报告」
    ↓
① 抽题：从「问题库」按 keyword_type（sys_dict）每类抽若干条 → 写入 geo_health_question
    （条数配置：GEO_HEALTH_QUESTIONS_PER_TYPE，默认每类 10，见 backend/src/config/geoBrandTaskConfig.js）
    ↓
② 探针：对「每道题 × 任务勾选的多条大模型连接」分别调用 AI → 写入 geo_health_answer（原始 JSON）
         → 解析回答里的 sources/articles → 去重写入 geo_health_article（引用条目）
    （实现：backend/src/services/geoBrandTaskService.js）
    ↓
③ 二次分析：逐条读取 geo_health_answer 中的回答正文，再调「分析模型」打多维标签
             → 写入 geo_health_analysis（与 answer 一对一；失败行可有 error_text）
    （实现：backend/src/services/geoBrandAnalysisService.js；Prompt：backend/src/prompts/geoHealthAnalysis.js）
    ↓
③b 词云快照：对本任务分析成功行对应的探针正文跑「词云 AI」（可关）→ 合并/计数后 **覆盖写入 geo_health_word_cloud_item**
    （实现：geoHealthWordCloudPersistService.js + sentimentWordCloudAiService.js；报告 GET 只读本表）
    ↓
④ 报告聚合：GET /api/geo-health-report 读取「当前用户下最新已完成任务」的全部 geo_health_analysis（error_text 为空）
             做 KPI、矩阵、竞品、词云等运算 → JSON 给前端；**命中 `geo_task_cache` 且指纹未变时直接返回快照（含词云），跳过重算**
```

**统计粒度：** 报告中绝大多数指标的分母是 **`geo_health_analysis` 中分析成功的行数**（`error_text IS NULL`），即「题 × 模型」级别；不是单纯的「题目数」。

**可选中止：** 用户在生成过程中可点击「中止生成」，前端调用 **`DELETE /api/geo-brand/tasks/:id`** 删除**未终局**任务行；依赖库表 **`ON DELETE CASCADE`** 级联删除本题 `question` / `answer` / `article` / `analysis` / 词云相关等子数据。后台探针若仍有在途 HTTP，无法瞬间取消，但写库前会 **`geoHealthTaskExists`** 判断任务是否仍存在，避免对已删任务继续 `INSERT`/`UPDATE`。详见下文「一点五」。

---

## 一点五、任务 API、后台编排与「中止生成」

实现入口：**路由** `backend/src/routes/geoBrandTask.js`（挂载在 **`/api`** 下）；**探针** `backend/src/services/geoBrandTaskService.js`；**分析** `backend/src/services/geoBrandAnalysisService.js`；**前端** `frontend/src/views/GEOHealthReport.vue`（创建后轮询进度 + 中止按钮）。

### 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/geo-brand/tasks` | 创建任务：抽题入库后立即返回 `taskId`；**探针 + 分析在后台异步执行**（`setImmediate`） |
| `GET` | `/api/geo-brand/tasks/:id/progress` | 轮询进度：总数、探针成功/失败数、分析进度、`status` 等；**任务不存在时 HTTP 404**（`success: false`） |
| `DELETE` | `/api/geo-brand/tasks/:id` | **中止未完成**：仅当任务属于当前 `x-user-id` 对应用户，且 **`status` 不在 `completed`、`failed`** 时删除 `geo_health_task` 一行；否则 400（已结束不可删）或 404（无此任务） |

### 后台执行顺序（与中止的衔接）

1. `POST` 成功后，同一进程内 **`runAllProbesForTask`**（滑动窗口并发探针）。  
2. 探针全部 worker 结束后：若 **`geo_health_task` 中已无该 `id`**（用户已中止删除），则**不再** `UPDATE probing_done`，也**不再调用** `runAllAnalysisForTask`。  
3. 若任务仍存在：进入 **`runAllAnalysisForTask`**（分析并发 → 词云快照 → `status = completed`）。分析过程中若任务被删：提前 **`return { aborted: true }`**，不写词云、不置 `completed`。  
4. 探针单题 **`probeOneQuestionWithModel`**：入口及写 `geo_health_answer` 前若任务已不存在，则跳过（返回 `null`），不把已删任务标为 `failed`。  
5. `runAllProbesForTask` 若发生**致命异常**：仅当任务行仍存在时，才把 `status` 更新为 `failed`（避免对已删任务写库）。

### 前端行为摘要

- 创建任务后轮询 **`GET .../progress`**；收到 **404** 视为任务已不存在，**结束轮询**并清理本地 `generating` / `activeTaskId` / `localStorage` 中的活动任务 id。  
- 用户确认「中止生成」后：**`DELETE`** 成功则置 **`pollAbort.cancelled = true`** 打断轮询循环；**仅当 `status === 'completed'`** 时提示成功并 **`loadHealthReport()`**，避免中止后仍弹「任务已完成」。  
- 页面恢复逻辑（`localStorage` 中的活动 `taskId`）：若进度接口返回 404，则清除缓存，不再误恢复已删任务。

---

## 二、涉及的核心表（报表与各模块各自主要读哪张表）

| 表名 | 作用 |
|------|------|
| `geo_health_task` | 任务头：`keyword`（创建时的品牌/企业名）、`connection_ids`（探针模型）、`analysis_connection_id`（分析模型）、`status`（completed 才参与报告） |
| `geo_health_question` | 本次抽检题干：`question`、`question_type`（对应 sys_dict 的 `data_key`） |
| `geo_health_answer` | 探针原始结果：`model_name`、`raw_json`（含回答正文，词云统计用）、`error_text`（探针失败） |
| `geo_health_article` | AI 回答中解析出的引用条目：`source_category`（四分类，信源饼图用）、`title`/`url` 等 |
| `geo_health_analysis` | 二次分析结果：**visibility / position / brand_status / compare_status / has_negative / competitors_mentioned / source_type** 等；**绝大部分 KPI 与矩阵直接读本表** |
| `geo_health_word_cloud_item` | **本期任务词云词条（持久化）**：`keyword` 展示词、`phrase_norm` 合并键、`tier`（即 positive/neutral/negative）、`hit_count`、`parent_id`（聚类子词挂主词）、`enabled`、`source`（`ai` 首次入库 / `user` 管理端编辑）。**主报告 `sentimentWordCloud` 只读本表**，不在 GET 时现场算词库命中。 |
| `geo_sentiment_lexicon` | **用户级全局情感词库**（种子与「情感词」配置）；**不**再作为报告词云列表的直接数据源；与本期词云条目的关系见模块 E。 |
| `geo_task_cache` | **报告快照**：按 `task_id` 唯一一行；存整份 `report_payload` 及 `sentiment_word_cloud`；指纹三段为 **分析成功行数+最后分析时间**、**文章行数+最后入库时间**、**本期 `geo_health_word_cloud_item` 行数+最后 `updated_at`**（列名仍为 `lexicon_fingerprint`），任一变化则下次 GET 重算并覆盖缓存。环境变量 `GEO_TASK_CACHE=0` 关闭读/写缓存。 |

---

## 三、报告选用「哪一次任务」的数据

接口逻辑（`geoHealthReport.js`）：

- 取 `users` 中当前 `x-user-id` 的 **企业名称** `company_name`，与任务表 `geo_health_task.keyword` 对齐（任务创建时写入）。
- 在 **`status = 'completed'`** 且「该任务下已有 `geo_health_analysis`」的任务中，按 **`geo_health_analysis.created_at` 最晚** 的任务取 **一条** 作为整页数据源。
- 若 `keyword` 为空（历史数据），仍可能与当前企业名称匹配。

因此：**报告永远是「该企业名下最近一次分析完成的体检任务」的全量聚合**，不是多任务混合。

---

## 四、`category`（brand / compare / open）怎么来的

**不由分析 AI 输出**，在入库分析结果前由代码 **`inferCategory(question_type, keyword_type_label)`** 判定（`geoBrandAnalysisService.js`）：

1. **优先**用 sys_dict 里 `keyword_type` 的 **`data_value`（展示文案）** 做关键词匹配：  
   - 含「对比」「竞品」→ `compare`  
   - 含「品牌词」「核心词」或「品牌」「核心」等 → `brand`  
   - 含「企业词」→ `brand`  
   - **其余** → `open`
2. **无字典文案时**按 `question_type`（`data_key`）兜底，例如默认种子：`01→brand`，`02→open`，`03→open`，`04→brand`，`05→compare`，`06→open`（与库内种子一致；若你们改过字典，以运行时的 `inferCategory` 为准）。

**含义：**  
- **`open`**：开放式意图（用于「首行心智拦截率」等 KPI 的主口径）。  
- **`brand`**：品牌向题干 → 矩阵走「品牌词」规则（`brand_status`）。  
- **`compare`**：对比向题干 → 矩阵走「对比词」规则（`compare_status`）。

> 旧文档写死的「01 核心词 / 02 对比词」可能与当前字典不一致；**以线上 `keyword_type` 字典 + 上述推断为准**。

---

## 五、模块说明（与页面模块对齐）

### 模块 A：大模型可见度综合得分（柱状图）

**数据源：** `geo_health_analysis`，`error_text IS NULL`，按 `model_name` 分组。

**公式：**

```text
某模型得分 = (该模型 visibility = 'visible' 的行数 / 该模型总行数) × 100，四舍五入到 0–100
```

**状态文案（柱子上标签）：**  
结合「可见度得分」与下方 **同一卡片上的 `healthScore`（AI 健康分）** 分档：≥70 表现良好；40–69 待加强；&lt;40 高风险（见模块 G）。

**子弹文案：** 根据该模型 `visibleCount/total` 与样本量是否 &lt;5 生成（`buildModelBullets`）。

---

### 模块 B：四大核心 KPI（首行心智拦截率、盲区指数、负面关联度、信源权威指数）

以下均在 **同一 `taskId`**、**`error_text IS NULL`** 的分析行上计算。

#### B1 首行心智拦截率（interceptRate）

- **优先分母：** `category = 'open'` 的分析行总数。  
- **分子：** 其中 `visibility = 'visible'` 的行数。  
- **公式：** `AVG(visible ? 100 : 0)`，再 clamp 到 0–100。  
- **若无开放式样本**（open 行数为 0）：**降级为全部分析行**（`kpiDenominator = 'all_fallback'`），前端可提示「开放式样本不足」类说明。

#### B2 大模型盲区指数（blindModelCount / totalModelCount + blindIndex）

- 对每个 `model_name` 单独看 **`category = 'open'`** 的行：  
  - 若 `open_total > 0` 且 **`open_blind == open_total`**（开放式全部 `not_visible`）→ 计为 **1 个盲区模型**。  
- **展示：** 分数形式「盲区模型数 / 参与模型数」；`blindIndex` 为盲区占比 ×100（供进度条）。

#### B3 负面事实关联度（negativeRatio / negativeRiskLevel）

- **分子：** `has_negative = true` 的分析行数。  
- **分母：** 全部分析行数。  
- **negativeRatio：** 0–1 小数（保留 4 位）。  
- **风险等级：** 0 健康；(0,0.1) 亚健康；[0.1,0.2) 低风险；[0.2,0.3) 高风险；≥0.3 超高风险。

#### B4 信源权威指数（authorityScore）

- **分母：** 全部分析行数 `totalChecks`（与分析成功条数一致）。  
- **分子：** `source_type` 命中「可信关键词」的行数之和。可信关键词固定包含：**官网、媒体、百科、认证、官方、新闻、期刊**（子串匹配）。  
- **公式：** `credibleCount / totalChecks × 100`。

#### B5 品牌提及率 vs 行业基准线（brandMentionRate / industryMentionRate）

仅 **`category = 'open'`** 的分析行：

- **指标 A（品牌提及率）：** `visibility = 'visible'` 行数 / open 总行数 × 100。  
- **指标 B（行业基准线）：** `(visibility = 'visible' OR competitors_mentioned 非空数组)` 行数 / open 总行数 × 100。  

**补充字段：**

- **`openMentionTotal`：** 开放式 **分析行数**（题×模型）。  
- **`openQuestionCount`：** 用 **`inferCategory(question_type, data_value) === 'open'`** 统计的**题目数**（与字典展示文案一致），用于说明「开放式题目有多少道」，与 `openMentionTotal` 含义不同。

---

### 模块 C：全域可见度矩阵（问题类型 × 模型）

**数据源：** `geo_health_analysis` 成功行；按 **`(question_type, model_name)`** 分桶，桶内可能有多行（多题）。

**行（意图路径）：** 来自 **`sys_dict.keyword_type`**（启用项），顺序与字典一致；无数据时用代码兜底键位。

**列：** 本任务出现过的 `model_name`，展示名见后端 `MODEL_DISPLAY_MAP`，并可叠加「大模型接入」里配置的 Logo。

**单元格规则（与代码一致）：**

| 大类 | 输入字段 | 规则摘要 |
|------|----------|----------|
| **open** | `position`, `has_negative` | T0=3 分，T1=1，T2=0；**T3 或 has_negative → 强制「负面风险」**；否则按均分 ≥2.5 / ≥1 / &gt;0 / 0 分档 |
| **brand** | `brand_status`, `has_negative` | accurate=3，bias=1；**hijack → 竞品劫持**；**risk 或 has_negative → 负面预警**；再按均分档 |
| **compare** | `compare_status`, `has_negative` | win=3，neutral=1，lose=0；**compare_status=hijack 或全 lose → 竞品挟持**；**risk 或 has_negative → 负面预警**；再按均分档 |

无数据单元格：`state = no_data`。

**矩阵底部汇总：** 统计各单元格 `state` 归属：绿色/蓝色类为「被拦截」；灰色缺失类为「盲区」；竞品占优/负面风险/挟持等为「竞品占优」侧（与前端映射一致）。

---

### 模块 D：竞品拦截诊断（帕累托图）

**数据源：** `geo_health_analysis.competitors_mentioned`（JSON 文本数组）逐元素展开。

- **柱高：** 某竞品名出现次数。  
- **占比 pct：** `该竞品次数 / 全竞品展开总次数 × 100`（整数百分比）。  
- **累计曲线：** 按次数降序前缀和 / 总次数。  
- **80% 虚线：** 帕累托解读用。

**点击某一竞品：** 前端请求 **`GET /api/geo-health-report/competitor?taskId=&name=`**，返回该竞品在不同 `question_type`、`model_name` 下的 **win/lose/neutral 计数**及 **has_negative** 计数，并附带各桶内**去重后的源问题**列表（题干优先来自 `geo_health_question`，否则回退 `questions` 源题）。

---

### 模块 E：AI 语义情绪（词云）

**与旧版的核心区别：** 主报告词云 **不是** 在每次 `GET /api/geo-health-report` 时用全局 **`geo_sentiment_lexicon`** 去扫探针原文临时拼出来的；也 **不是** 简单汇总分析 AI 的 `sentiment_keywords` 字段。当前口径是：**任务完成时把「AI 抽取短语（可选）+ 在原文上汇总的命中次数」写入表 `geo_health_word_cloud_item`，报告只读该表**。

#### E1 读路径（报告接口）

- **`GET /api/geo-health-report`** 中 `sentimentWordCloud`：由 **`loadPersistedWordCloudPayload`**（`backend/src/services/geoHealthWordCloudPersistService.js`）查询 **`geo_health_word_cloud_item`**（`task_id` 匹配且 **`enabled = true`**），映射为 `text / count / polarity / weight / source`；**`weight` = 本任务各词条 `hit_count` 中的最大值归一化**。
- 若表内无行或全被禁用，词云为空数组。

#### E2 写路径（任务完成时，覆盖式快照）

- **触发时机：** 二次分析 **`runAllAnalysisForTask`** 在将 `geo_health_task.status` 置为 **`completed` 之前**，调用 **`persistWordCloudSnapshot` → `persistAiWordCloudForTask`**（`geoBrandAnalysisService.js` → `geoHealthWordCloudPersistService.js`）。词云入库失败会打日志，**不阻塞**任务置完成（与代码一致）。
- **输入数据：** 本任务 **`geo_health_analysis.error_text IS NULL`** 的行，联表 **`geo_health_answer.raw_json`**，用 **`extractProbeAnswerText`** 取探针回答正文（与二次分析读到的正文同源）。
- **AI 阶段（可关）：** **`fetchWordCloudPhrasesFromAi`**（`sentimentWordCloudAiService.js`）  
  - 使用当前用户下 **第一条 `enabled` 的大模型连接**（与「分析模型」不是同一条逻辑线）。  
  - 环境变量 **`GEO_HEALTH_WORDCLOUD_AI=0`**（或 `false`/`off`）→ **不调模型**，后续仅依赖合并结果（通常为空）。  
  - 批参数：`GEO_HEALTH_WORDCLOUD_AI_MAX_CHARS`、`GEO_HEALTH_WORDCLOUD_AI_MAX_ANSWERS`；超时 `GEO_HEALTH_WORDCLOUD_AI_TIMEOUT_MS`；简介/受众注入截断：`GEO_HEALTH_WORDCLOUD_BRAND_BRIEF_MAX_CHARS`、`GEO_HEALTH_WORDCLOUD_TARGET_AUDIENCE_MAX_CHARS`（见 `geoHealthWordCloudAi.js`）。  
  - Prompt 要求模型按条回答返回 JSON：`items[].answerId`（对应 **`geo_health_analysis.id`**）、`phrases[]` 含 **`phrase`、`polarity`（positive|neutral|negative）、`clusterHead`（可选）**。  
  - 服务端 **`normalizeAiItems`**：丢弃超长（须满足 **`isSentimentKeywordLengthValid`**，与情感词一致：**≤4 个 Unicode 码点**）；**`phrase` 必须逐字出现在该条回答原文中**；`clusterHead` 也须在原文中且与 `phrase` 区分、合法，否则清空。
- **合并与计数：** **`mergeLexiconAndAiWordCloud`**（`sentimentWordCloudAiService.js`）  
  - 多批 AI 结果先 **`aggregateAiPhraseBuckets`**：按 **`mergeKeyForWordCloudPhrase`**（含拉丁字母则小写）+ **极性** 合并短语；对 **`clusterHead` 投票**，且主词必须出现在「本任务短语集合」中才保留，避免悬空父节点。  
  - 对每条进入结果的 AI **`phrase`**，在**本任务全部回答正文**上调用 **`countLexiconPhraseInText`** 累加出现次数，得到 **`hit_count`**（函数名历史遗留，语义为「短语在原文中的规则化子串计数」，不是读全局词库表）。  
  - **当前 `persistAiWordCloudForTask` 固定传入 `lexEntries: []`**：即任务**首次入库快照不包含**全局 **`geo_sentiment_lexicon`** 的词条与 AI 去重合并；若产品将来要在入库时合并词库，可改为传入真实 `lexEntries`/`lexHitCount`（合并函数已支持：**先词库命中且 count>0，再 AI；同合并键不双计，极性以词库为准**）。
- **入库：** 事务内 **`DELETE FROM geo_health_word_cloud_item WHERE task_id = ?`** 后，按 **`orderWordCloudItemsForPersist`** 的深度优先顺序（**主词 → 子词**）`INSERT`；子行 **`parent_id`** 指向同档同极性下的主词行；**`source` 恒为 `ai`**。

#### E3 聚类（`clusterHead` 与 `parent_id`）

- 模型可在同一极性下指定 **`clusterHead`**，表示该短语归并到某一「代表词」下展示。  
- 入库时 **`orderWordCloudItemsForPersist`** 保证父节点先于子节点插入，以便写入 **`parent_id`**；库表自引用 **`ON DELETE CASCADE`**，删父时子级一并删除。

#### E4 全局词库 `geo_sentiment_lexicon` 与「情感词管理」

- **`geo_sentiment_lexicon`**：用户维度全局词表（种子、校验、运营配置等），**不**再直接驱动报告页 `sentimentWordCloud` 数组的生成。  
- **任务级维护**：后台 **`/api/sentiment-lexicon/*`**（见 `sentimentLexicon.js`）对 **`geo_health_word_cloud_item`** 增删改、近义词合并、子词挂载等；人工维护后 **`source` 可为 `user`**。  
- **`POST /api/sentiment-lexicon/rebuild-word-cloud`**：对「当前用户最近一条已完成任务」**重新跑与任务完成时相同的 AI persist**，**整表覆盖**该 `task_id` 下的词云行（耗时可能较长）。

#### E5 下钻明细 `GET /api/geo-health-report/sentiment-sources`

- 词条集合来自 **`loadWordCloudLexEntriesForTask`**（读本期 **`geo_health_word_cloud_item`** 的 `keyword` + `tier`），再在探针原文上展开「哪道题命中了哪个词」。**因此下钻与主报告词云同源**；用户在管理页为本期任务新增/禁用的词会反映在下钻列表中。

#### E6 缓存与中止

- **`geo_task_cache`** 第三段指纹对应 **`geo_health_word_cloud_item`** 行数与 **`max(updated_at)`**（实现见 `geoTaskCacheService.js`），词云表变更会使整份报告缓存失效。  
- 中止/删除 **`geo_health_task`** 时，外键 **`ON DELETE CASCADE`** 会删除本期 **`geo_health_word_cloud_item`**（及 `geo_task_cache` 等）。

#### E7 颜色

- 词条极性来自库字段 **`tier`**（接口输出为 `polarity`）：**positive / neutral / negative**，前端按三档着色。

---

### 模块 F：底层信源溯源（条形图 / 饼图）

**与旧版说明的重要区别：** 主报告里的 **`sourceData`** 来自 **`geo_health_article.source_category` 聚合**，不是 `geo_health_analysis.source_type` 的 Top8。

**四分类枚举（与探针解析一致）：**

| 内部值 | 展示名 |
|--------|--------|
| `authority_media` | 权威媒体 |
| `industry_vertical` | 行业垂直 |
| `official_media` | 官方自媒体 |
| `ugc_community` | UGC / 社区 |

饼图占比：**四类条数之和为分母**（缺类视为 0）；未归类入库可能记为 `industry_vertical` 等默认策略（见 `sourceClassifier.js`）。

**「采信率」类文案：** 若页面与 **KPI「信源权威指数」** 对照，后者仍基于分析行的 **`source_type` 文本** 与可信关键词（见模块 B4），与饼图的 **`geo_health_article` 分类** 口径不同，**勿混为一谈**。

**文章列表下钻：** **`GET /api/geo-health-report/source-articles?taskId=&page=&q=`**（标题、URL、分类、对应题干）。

---

### 模块 G：综合健康分（顶部大字 healthScore）

**与旧版「四项加权」公式不一致**；当前以 **`backend/src/utils/aiHealthScore.js`** 为准：

```text
记 v = 当前「卡片所用模型」的可见度得分（0–100，单模型即该柱得分）
记 ir = 任务级「首行心智拦截率」interceptRate（0–100）
记 n = 任务级 negativeRatio（0–1）
记 r = industryMentionRate > 0 ? brandMentionRate / industryMentionRate : 0

raw = v × 0.4 + ir × 0.6 − n × v − (n × r) × 100
healthScore = clamp(raw, 0, 100)
```

- 接口里 **根字段 `healthScore`** 与 **`modelVisibilityCards[0].healthScore`** 一致（按可见度排序后的第一个模型卡片）。  
- **每张模型卡片**上的 `healthScore` 用 **该模型自己的可见度** 代入 `v`，`ir/n/brandMentionRate/industryMentionRate` 仍为**任务级同一套**。

**comparePercent：** `max(5, 100 - healthScore) + '%'`（兼容字段，语义为「对比/差距」类展示）。

---

### 模块 H：智能诊断总结（diagnosticSuggestions）

**与旧版「多条硬规则表格」不一致。** 当前后端 **仅**根据 **综合 AI 语境矩阵（16 档）** 生成 **至多一条** 诊断：

- 输入：各模型可见度得分数组、`interceptRate`、`negativeRatio`。  
- 对每个模型先算 `exposure = 0.4×vis + 0.6×ir`，`penalty = negativeRatio`，映射到 16 档之一（`backend/src/utils/contextMatrix.js`）。  
- 再对多模型的 exposure / penalty **取平均**，落到同一套 16 档，得到 **`matrixContext`**（名称、摘要、建议列表）。  
- **`diagnosticSuggestions`** = 把该档位包装成一条卡片（若吸附到最近邻档位会附加说明文案）。

页面若支持编辑文案，一般为**前端本地展示**，不回写库（以产品实现为准）。

---

## 六、辅助接口（排查数据 / 对账）

### 6.1 报告与对账（geo-health-report）

| 接口 | 用途 |
|------|------|
| `GET /api/geo-health-report/task-qa?taskId=` | 本次任务全部「题 × 模型」的题干 + 探针回答原文 + 错误信息，便于与矩阵、词云对账 |
| `GET /api/geo-health-report/sentiment-sources?taskId=&page=&q=` | 词云命中明细（词条来自本期 **`geo_health_word_cloud_item`**，在探针原文上展开） |
| `GET /api/geo-health-report/source-articles?taskId=&page=&q=` | 引用文章明细 |
| `GET /api/geo-health-report/competitor?taskId=&name=` | 单个竞品详情 |

### 6.2 体检任务编排（geo-brand）

| 接口 | 用途 |
|------|------|
| `POST /api/geo-brand/tasks` | 创建体检任务并启动后台探针 + 分析 |
| `GET /api/geo-brand/tasks/:id/progress` | 前端轮询任务进度 |
| `DELETE /api/geo-brand/tasks/:id` | 中止未完成：删任务头，子表由外键级联清理 |
| `GET /api/geo-brand/tasks/:id` | 单任务基本信息 |
| `GET /api/geo-brand/tasks/:id/questions` | 任务题目列表（若需排查） |
| `GET /api/geo-brand/tasks/:id/results` | 任务探针结果摘要（若需排查） |

### 6.3 任务词云条目（sentiment-lexicon）

与 **`geo_health_word_cloud_item`** 读写相关，详见模块 E；常用排查接口示例：

| 接口 | 用途 |
|------|------|
| `POST /api/sentiment-lexicon/rebuild-word-cloud` | 对当前用户**最近一条已完成任务**重新执行与任务完成时相同的 AI 词云 persist，**覆盖**该 `task_id` 下词云表 |
| 其余 `GET/POST/PUT/DELETE /api/sentiment-lexicon/...` | 本期任务词条树、追溯、人工增删改等（实现见 `backend/src/routes/sentimentLexicon.js`） |

---

## 七、常见「看不懂」的问题

1. **为什么 KPI 分母和题目数不一致？**  
   多为「每题测了多个模型」，分析行数 = 题 × 模型；部分 KPI 还用 `category='open'` 再滤一层。

2. **为什么开放式题目数为 `openQuestionCount`，但开放式提及总行数是 `openMentionTotal`？**  
   前者是**题**，后者是**分析行（题×模型）**。

3. **探针失败或分析失败的题会怎样？**  
   探针失败：`geo_health_answer.error_text` 有值，可能无分析行或分析行带错。  
   报告聚合 **忽略** `geo_health_analysis.error_text IS NOT NULL` 的行，故失败题不参与 KPI/矩阵。

4. **词云没有词？**  
   先看 **`GEO_HEALTH_WORDCLOUD_AI`** 是否关闭、用户是否有 **enabled 的大模型连接**；再看任务完成后 **`geo_health_word_cloud_item`** 是否有行、是否全被 **`enabled=false`**；并确认分析成功行对应 **`raw_json`** 中是否有可抽取正文。全局 **`geo_sentiment_lexicon`** 配置**不**再自动填满报告词云列表（见模块 E）。

5. **「中止生成」后为什么后台可能还会打一会儿 AI？**  
   中止 = 删库任务行 + 前端停止轮询；**已发出的探针/分析 HTTP 请求无法由服务端强制撤回**。写库路径已用「任务是否仍存在」做守卫，减少对已删任务的写入与报错。

6. **为什么已 `completed` / `failed` 的任务不能 `DELETE` 中止？**  
   接口约定：**仅未终局任务**可删，避免误删已用于报告的历史任务；要看历史报告仍走 `GET /api/geo-health-report`。

---

## 八、附：数据流简图

```
问题库 ──抽题──► geo_health_question
                    │
多模型探针 ───────► geo_health_answer + geo_health_article
        （可选：DELETE /api/geo-brand/tasks/:id ──► 删 geo_health_task，子表 CASCADE）
                    │
分析模型打标 ─────► geo_health_analysis
                    │
词云 AI 快照 ─────► geo_health_word_cloud_item（任务完成时覆盖写入；中止任务时随 task CASCADE 删除）
                    │
GET /api/geo-health-report ──► 前端各模块
```

---

*文档更新：2026-05-11 · 主要对齐代码：`backend/src/routes/geoHealthReport.js`、`backend/src/routes/geoBrandTask.js`、`backend/src/routes/sentimentLexicon.js`、`backend/src/services/geoBrandTaskService.js`、`backend/src/services/geoBrandAnalysisService.js`、`backend/src/services/geoHealthWordCloudPersistService.js`、`backend/src/services/sentimentWordCloudAiService.js`、`backend/src/services/geoTaskCacheService.js`、`frontend/src/views/GEOHealthReport.vue`、`backend/src/utils/aiHealthScore.js`、`backend/src/utils/contextMatrix.js`、`backend/src/services/sentimentLexiconService.js`*
