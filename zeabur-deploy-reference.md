# Zeabur 部署参考（已废弃）

> ⚠️ 已废弃，仅供参考。实际部署使用根目录 `zeabur.json`。

## 文件用途

此文件是早期手动部署参考，与当前 Zeabur 配置不兼容。

## 与实际配置的矛盾

| 字段 | 本文件值 | 实际值 |
|------|---------|--------|
| port | 3000 | 后端实际用 3001（见 `.env`） |
| 适用服务 | 不明确 | 应为 frontend 服务 |

## 当前实际配置

根目录 `zeabur.json` 是唯一的有效 Zeabur 配置文件。
